{
  "name": "build",
  "version": "0.0.0-dev",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:static": "node src/build-static.ts",
    "build": "node src/build.ts",
    "test": "node --test test/*.test.ts"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
